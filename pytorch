i.Create conda environment

Command: conda create -n pyt --clone base

ii.Activate the conda environment

Command: conda activate pyt

iii.Install PyTorch

Command: conda install pytorch torchvision cpuonly -c pytorch

iv. Test the installation in python interpreter
Command: python
from __future__ import print_function
import torch
x = torch.rand(5, 3)
print(x)

v.Deactivate the environment to go to base environment
